import { FilterStockBajoPipe } from './filter-stock-bajo.pipe';

describe('FilterStockBajoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterStockBajoPipe();
    expect(pipe).toBeTruthy();
  });
});
