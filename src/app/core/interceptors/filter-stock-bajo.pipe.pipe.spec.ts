import { FilterStockBajoPipePipe } from './filter-stock-bajo.pipe.pipe';

describe('FilterStockBajoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterStockBajoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
